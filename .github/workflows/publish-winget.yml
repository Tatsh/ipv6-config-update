jobs:
  update-winget:
    runs-on: 'windows-latest'
    steps:
      - uses: 'vedantmgoyal9/winget-releaser@main'
        with:
          identifier: 'Tatsh.ipv6-config-update'
          max-versions-to-keep: 3
          token: '${{ secrets.WINGET_TOKEN }}'
name: 'Publish to WinGet'
'on':
  release:
    types:
      - 'released'
